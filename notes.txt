Database update:
npx prisma db push

https://icones.js.org/

Make a view for boss and worker

Cashier
- Print receipt

Show log 

I need to see if one query suffice or if i need to wrap one query into a transaction to prevent unnecessary changes

ophthalmologists 


//Rip virtual tables doesn't support touch screen
<el-auto-resizer>
  <template #default="{ height, width }">
    <el-table-v2 :data="store.inventory.stock" 
    :columns="store.columns.map((column, index) => ({key: index, title: column, dataKey: column, width: 200}))" 
    :width="width" :height="height" fixed :estimated-row-height="40" row-class="table-row" @row-click="handleRowClick">
      <template #empty>
        <div class="table-template">
          <el-empty />
        </div>
      </template>
    </el-table-v2>
  </template>
</el-auto-resizer>

<style lang="scss">
#wrapper {
  &.edit-mode .table-row {
    cursor: pointer;
    &:hover {
      background: #ffff0026 !important;
    }
    div {
      user-select: none;
    }
  }
}

.el-table-v2__body > div:nth-child(1) {
  overflow: auto !important
}

.el-virtual-scrollbar {
  display: none;
}
</style>









frostedfreeze.com

waffle


Need to add permission for to see log or not for workers
Need to add permission to see inventory
Need to add permission to see cashier

Need to bug test

Then need to ask what actions are to be added, add to permissions, add to log



Store viewer for worker